# Core Deep Learning Libraries
# Note: Install PyTorch manually based on your CUDA version for GPU support
# (see https://pytorch.org/get-started/locally/). For CPU-only, use 'torch==2.5.1+cpu'.
torch==2.5.1  # Use 'torch==2.5.1+cpu' for CPU-only environments
transformers==4.53.2
datasets==4.0.0

# Core Data Science and ML Libraries
scikit-learn==1.7.0
numpy==2.1.2
pandas==2.3.1

# For data processing and file handling
pyarrow==20.0.0
dill==0.3.8
fsspec==2024.6.1
huggingface-hub==0.33.4
safetensors==0.5.3

# Utilities
tqdm==4.67.1
